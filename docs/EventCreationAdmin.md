# Event Creation Requirements

The **Add Event** screen requires a few key fields before an event can be saved.
Administrators must provide:

- Event Name
- Date
- Start Time
- Venue Name

If any of these are missing the form displays an error and the record is not
inserted. The matching inputs on the form also include the `required` attribute
for basic browser validation.
